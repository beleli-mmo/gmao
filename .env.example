# ── API ─────────────────────────────────────────────
DATABASE_URL="postgresql://gmao:gmao@localhost:5432/gmao?schema=public"
PORT=4000
CORS_ORIGINS="http://localhost:5173,http://localhost:3000"
JWT_ACCESS_SECRET="change-me-access"
JWT_REFRESH_SECRET="change-me-refresh"
DEFAULT_LABOR_RATE=12000       # XOF / heure mécanicien interne
TRAVEL_KM_RATE=350             # XOF / km

# ── CouchDB (cible de réplication PouchDB) ──────────
COUCH_URL="http://admin:admin@localhost:5984"
COUCH_FIELD_DB="field-tickets"
COUCH_META_DB="sync-meta"

# ── Stockage objets (MinIO / S3) ───────────────────
S3_ENDPOINT="http://localhost:9000"
S3_REGION="us-east-1"
S3_BUCKET="gmao-media"
S3_ACCESS_KEY="minio"
S3_SECRET_KEY="minio12345"

# ── PWA (Vite) ─────────────────────────────────────
VITE_COUCH_URL="http://localhost:5984"
VITE_API_URL="http://localhost:4000"
