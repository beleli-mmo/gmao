# Admin web — variables Vercel (Project → Settings → Environment Variables)
NEXT_PUBLIC_API_URL=https://gmao-api.vercel.app

# Laisser VIDE si l'API est sur Vercel (pas de WebSocket → rafraîchissement par polling).
# Renseigner uniquement si l'API tourne en process long (Render/VPS) : wss://gmao-api.onrender.com/ws
# NEXT_PUBLIC_WS_URL=
